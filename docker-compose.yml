version: '3.8'

services:
  app:
    build: .
    ports:
      - "6700:6700"
    environment:
      - PYTHONPATH=/app
    env_file:
      - stack.env
    # volumes:
    #   - ./src:/app/src
    restart: unless-stopped
    networks:
      - norseai

networks:
  norseai:
    external: true